<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--<declare-styleable name="CustomRatingBar">-->
    <!--<attr name="maxStars" format="integer"/>-->
    <!--<attr name="stars" format="float"/>-->
    <!--<attr name="starOff" format="reference|integer"/>-->
    <!--<attr name="starOn" format="reference|integer"/>-->
    <!--<attr name="starHalf" format="reference|integer"/>-->
    <!--<attr name="starPadding" format="dimension"/>-->
    <!--<attr name="onlyForDisplay" format="boolean"/>-->
    <!--<attr name="halfStars" format="boolean"/>-->
    <!--</declare-styleable>-->


    <declare-styleable name="RatingBar">
        <!-- The number of stars (or rating items) to show. -->
        <attr name="numStars" format="integer" />
        <!-- The rating to set by default. -->
        <attr name="rating" format="float" />
        <!-- The step size of the rating. -->
        <attr name="stepSize" format="float" />
        <!-- Whether this rating bar is an indicator (and non-changeable by the user). -->
        <attr name="isIndicator" format="boolean" />
        <!-- The spacing between items. -->
        <attr name="spacing" format="dimension" />
        <!-- What kind of mode for automatic adaptation. -->
        <attr name="automatic" format="enum">
            <!-- Fully automatic  -->
            <enum name="fully" value="0" />
            <!-- Semi automatic  -->
            <enum name="semi" value="1" />
            <!-- Manual -->
            <enum name="manual" value="2" />
        </attr>
        <!-- The default background image. -->
        <attr name="progress" format="reference" />
        <!-- The progressed background image. -->
        <attr name="progressed" format="reference" />
    </declare-styleable>
</resources>